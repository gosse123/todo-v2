[package]
name = "todo-cli-app"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
colored = "3.1.1"
dialoguer = "0.12.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
